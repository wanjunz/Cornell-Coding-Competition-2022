#include <stdio.h>

int main(void)
{
    int a;
    int b;
    int square;
    int ts;
    scanf("%d", &a);
    scanf("%d", &b);
    square = (a * b);
    ts = square * square;
    printf("%i", ts);
}